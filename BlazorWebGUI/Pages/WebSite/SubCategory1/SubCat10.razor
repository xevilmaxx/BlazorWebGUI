@page "/subcat10"
@attribute [Authorize(Policy = "Test0")]

@*Original Idea from:
https://www.youtube.com/watch?v=EMWfM3_v0J4*@

<MudTabs Elevation="0" Rounded="true" ApplyEffectsToContainer="true" PanelClass="pa-6" Class="ml-0">

	@foreach (var item in tabs)
	{
	 <MudTabPanel Text="@item.Key">
		@Helpers.DynamicRender.RenderRazorComponent(item.Value)
	 </MudTabPanel>
	}

</MudTabs>

@code {

	//int selected = 0;

	//ComponentBase[] components = { new SubCat11(), new SubCat12() };

	//Type[] types => components.Select(x => x.GetType()).ToArray();

	Dictionary<string, Type> tabs = new Dictionary<string, Type>()
	{
		{"Tab SubCat11", typeof(SubCat11)},
		{"Tab SubCat12", typeof(SubCat12)}
	};

}